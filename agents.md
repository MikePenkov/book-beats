# 项目记忆与行为规范

## 1. 项目使命、范围与禁止事项

- 本项目部署并运行 `MikePenkov/book-beats`。
- 未经用户明确要求，不修改上游业务功能、不提交或推送远程仓库、不暴露密钥。

## 2. 代码风格、命名规范、架构决策

- 优先遵循上游仓库既有规范与结构。
- 采用最小可用改动；避免无需求的抽象、依赖和配置。

## 3. 技术栈偏好与最佳实践

- 以仓库 README、锁文件和现有脚本定义的运行方式为准。
- 先复用已有依赖与命令，再考虑新增工具。

## 4. 历史坑、教训与长期记忆

- 2026-08-31：项目目录初始为空，等待部署上游仓库。
- 2026-08-31：已部署上游 `main` 分支（`baa3f4a`），使用 Python 3.12 的项目内 `.venv` 安装依赖。
- 运行需在 `.env` 中配置 `CLIENT_ID`、`CLIENT_SECRET` 和 `OPENAI_API_KEY`；该文件必须保持不提交。
- 2026-08-31：本地功能分支 `feat/book-research-web-ui` 已改为 Flask Web UI；OAuth token、检索结果和预览仅保存于进程内存，表单设置（含密钥）保存于用户浏览器本地存储，直到用户主动清除。
- Web UI 支持中英切换、OpenAI / DeepSeek 预设及其他 OpenAI 兼容 API、手动或按书籍篇幅/预计阅读时长智能推荐歌曲数量、公开网页摘要研究及 Spotify 创建前预览确认；回调地址固定为 `http://127.0.0.1:5000/callback`。
- Spotify 歌单写入必须使用当前的 `POST /v1/playlists/{id}/items` 接口；旧的 `/tracks` 接口已弃用且会导致写入失败。
- 生成内容语言必须绑定用户开始授权时所选的 Web UI 语言；Spotify 歌单简介写入完整的“氛围名 — 氛围摘要”，并限制在 Spotify 的 300 字符上限内。
- 模型生成请求使用 `response_format={"type":"json_object"}` 强制结构化输出；生成失败时可在加载页重试，优先复用已换取的 Spotify access token。
- 智能歌曲数量不设人工上限；Spotify `/items` 单次最多 100 首，因此创建歌单时必须按 100 首分批提交。
- Spotify 授权回调后的检索、模型生成与歌曲匹配在 `/generating` 加载页中异步执行，必须保留阶段性提示，避免用户误以为页面卡死。
- 2026-08-31：用户确认体验后已将功能分支推送至 Fork，并向上游提交 PR #1：https://github.com/MikePenkov/book-beats/pull/1
- 2026-08-31：新增 Windows 双击启动入口 `start_book_beats.bat`；脚本会检测并重建失效 `.venv`、按需安装依赖、启动 Flask 服务并打开 `http://127.0.0.1:5000`。
- 2026-08-31：模型接口读取超时由 45 秒调整为 120 秒；超时时返回可重试的本地化提示，避免把底层连接堆栈直接展示给用户。
- 2026-08-31：修复预览页重复提交导致重复创建 Spotify 歌单；前端提交后禁用按钮，后端为每次运行增加一次性创建占用保护。
- 2026-09-01：重写 README，补充面向初学者的启动、Spotify 配置、模型设置、使用流程、隐私说明和常见问题。
- 2026-08-31：功能分支已快进合并到 Fork 的 `main`，并从 `klein523666:main` 向原项目提交 PR #2：https://github.com/MikePenkov/book-beats/pull/2；原功能分支 PR #1 仍保持开放。

## 5. 工作流程与质量标准

- 每次任务开始先阅读本文件。
- 部署后至少完成依赖安装和一个可复现的启动或构建检查。
- 重大规范变更需先向用户说明理由并获得确认。
